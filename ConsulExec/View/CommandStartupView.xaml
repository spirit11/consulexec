<UserControl x:Class="ConsulExec.View.CommandStartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ConsulExec.View"
             xmlns:vm="clr-namespace:ConsulExec.ViewModel"
             d:DataContext="{d:DesignInstance vm:CommandStartupViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.InputBindings>
        <KeyBinding Gesture="Ctrl+Z" Command="{Binding UndoCommand}"/>
    </UserControl.InputBindings>
    <StackPanel>
        <DockPanel>
            <Label Content="Execution Profile:"/>
            <Button Command="{Binding Profiles.EditCommand}" Content="Edit" DockPanel.Dock="Right"/>
            <Button Command="{Binding Profiles.DeleteCommand}" Content="Delete" DockPanel.Dock="Right"/>
            <Button Command="{Binding Profiles.AddCommand}" Content="Add" DockPanel.Dock="Right"/>
            <ComboBox ItemsSource="{Binding Profiles.List}" SelectedItem="{Binding Profiles.Profile}" DisplayMemberPath="Name" />
        </DockPanel>
        <DockPanel>
            <Button Command="{Binding ExecuteCommand}" Content="Execute" DockPanel.Dock="Right"/>
            <ComboBox Text="{Binding Command, UpdateSourceTrigger=PropertyChanged}" ItemsSource="{Binding RecentCommands}" IsEditable="True"/>
        </DockPanel>
        <Button Name="UndoButton" Content="Undo"/>
    </StackPanel>
</UserControl>

